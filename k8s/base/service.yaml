apiVersion: v1
kind: Service
metadata:
  name: aetherion
  namespace: aetherion
  labels:
    app.kubernetes.io/name: aetherion
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aetherion
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
